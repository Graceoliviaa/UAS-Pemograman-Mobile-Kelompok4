<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UASKelompok4">

        <activity
            android:name=".LoginActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".MainActivity" />

        <activity android:name=".ProjectListActivity" />
        <activity android:name=".ProjectDetailActivity" />
        <activity android:name=".ProjectFormActivity" />

        <activity android:name=".ClientListActivity" />
        <activity android:name=".ClientFormActivity" />

        <activity android:name=".ProjectTeamActivity" />
        <activity android:name=".AssignMemberActivity" />

        <activity android:name=".ProjectDocumentActivity" />
        <activity android:name=".DocumentFormActivity" />

    </application>

</manifest>